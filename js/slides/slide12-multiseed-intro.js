/* ============================================================
   SLIDE 12 — Multi-Seed Experiment: Overview & Parameters
   No D3 chart — content is static HTML; KaTeX renders on load.
   ============================================================ */

export function initSlide12() {
  /* KaTeX auto-renders LaTeX in the static HTML already.
     Nothing extra needed here. */
}
